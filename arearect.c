#include <stdio.h>
int main()
{
    int area;
    int length;
    int breadth;
    printf("enter a length of rectangle\n");
    scanf("%d", &length);
    printf("enter a breadth of reactagle\n");
    scanf("%d", &breadth);
    printf("area is %d", length * breadth);
    
}